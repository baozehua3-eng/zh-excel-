# 部署方案指南

## 🔍 问题分析

Vercel 部署遇到权限问题（错误代码 126），这通常是 Vercel 环境中的权限限制导致的。

## ✅ 方案一：使用 Netlify 部署（推荐，更简单）

Netlify 对 Vite 项目的支持很好，通常不会遇到权限问题。

### 步骤：

1. **访问 Netlify**
   - 打开：https://www.netlify.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add new site" → "Import an existing project"
   - 选择 "Deploy with GitHub"
   - 授权后，选择你的仓库 `zh-excel-`

3. **配置构建设置**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - 点击 "Deploy site"

4. **等待部署完成**
   - 通常 1-2 分钟
   - 会得到一个 `https://xxx.netlify.app` 的链接

---

## ✅ 方案二：使用 Cloudflare Pages（免费，速度快）

### 步骤：

1. **访问 Cloudflare Pages**
   - 打开：https://pages.cloudflare.com
   - 使用 GitHub 账号登录

2. **连接仓库**
   - 点击 "Create a project"
   - 选择你的 GitHub 仓库

3. **配置构建**
   - Framework preset: `Vite`
   - Build command: `npm run build`
   - Build output directory: `dist`

4. **部署**
   - 点击 "Save and Deploy"
   - 等待完成

---

## ✅ 方案三：本地构建后手动上传

如果在线部署一直失败，可以本地构建后上传到静态托管服务。

### 步骤：

1. **本地构建**
   ```bash
   npm run build
   ```
   这会生成 `dist` 文件夹

2. **上传到静态托管**
   - **Netlify**: 直接拖拽 `dist` 文件夹到 https://app.netlify.com/drop
   - **Vercel**: 在项目设置中选择 "Upload" 方式，上传 `dist` 文件夹
   - **GitHub Pages**: 将 `dist` 文件夹内容推送到 `gh-pages` 分支

---

## ✅ 方案四：修复 Vercel 配置

如果坚持使用 Vercel，尝试以下配置：

### 在 Vercel 项目设置中：

1. **Settings** → **General** → **Build & Development Settings**
2. **Build Command** 改为：
   ```
   npm install && npm run build
   ```
3. **Output Directory**: `dist`
4. **Install Command**: 留空（使用默认）
5. **Node.js Version**: 选择 `18.x` 或 `20.x`
6. 保存后重新部署

---

## 🎯 推荐方案

**优先尝试 Netlify**，因为：
- ✅ 对 Vite 项目支持更好
- ✅ 很少遇到权限问题
- ✅ 配置更简单
- ✅ 免费且稳定

---

## 📝 快速操作

### 使用 Netlify（最简单）：

1. 访问 https://www.netlify.com
2. 登录（GitHub 账号）
3. "Add new site" → "Import an existing project"
4. 选择仓库 `zh-excel-`
5. 设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
6. 点击 "Deploy site"
7. 等待 1-2 分钟，获得链接！

---

## 💡 如果所有方案都失败

可以尝试：
1. 检查 GitHub 上的代码是否最新
2. 确保 `package.json` 中的构建命令正确
3. 尝试使用不同的 Node.js 版本
4. 联系平台支持
